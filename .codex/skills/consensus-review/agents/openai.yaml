interface:
  display_name: "Consensus Review"
  short_description: "Multi-model cross review of a diff"
  default_prompt: "Use $consensus-review to cross-check my current diff with Claude, Codex and Gemini."
policy:
  allow_implicit_invocation: true
